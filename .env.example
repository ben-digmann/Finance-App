# Database (used by Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=finance_app

# Backend
NODE_ENV=development
PORT=8000
API_BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database Configuration (direct connection)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finance_app
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL=postgres://postgres:password@db:5432/finance_app

# JWT
JWT_SECRET=your_jwt_secret_for_development
JWT_EXPIRES_IN=7d

# Plaid API (NEVER commit real values to git)
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_sandbox_secret
PLAID_ENV=sandbox
PLAID_WEBHOOK_SECRET=optional_webhook_secret_for_production

# OpenAI API (optional)
OPENAI_API_KEY=your_openai_api_key
# Custom LLM (optional)
LLM_API_URL=https://s5rs0cklk8.execute-api.us-east-1.amazonaws.com/Prod/v1/chat/completions
LLM_API_TOKEN=your_llm_token

# Frontend
REACT_APP_API_URL=http://localhost:8000/api

# Logging
LOG_LEVEL=info